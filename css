/* Styles for the header */
header {
    background-color: #ffffff;
    padding: 20px;
    text-align: center;
    color: white;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
}

.img-header {
    float: left; /* Aligns the image to the left */
    margin-right: 15px; /* Adds space to the right of the image */
    margin-bottom: 15px; /* Adds space to the bottom of the image */
}

header h1 {
    font-size: 2.5rem;
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 2px;
}

/* General Styles */
body {
    font-family: Arial, sans-serif;
    background-color: #f0f0f0;
    color: #333;
    margin: 0;
    padding: 20px;
}

h1 {
    text-align: center;
    font-size: 2.5rem;
    color: #030303;
    margin-bottom: 20px;
    font-family: Georgia;
}

/* Styles for lists and preformatted text */
pre {
    background-color: #fff;
    border: 1px solid #ddd;
    padding: 15px;
    border-radius: 5px;
    overflow-x: auto;
    font-size: 1rem;
}

ol {
    margin-left: 20px;
}

i {
    color: #0c0165;
}

/* First list styles */
div {
    margin-bottom: 30px;
}

.div_2 {
    background-color: #f9f9f9;
    border: 1px solid #ddd;
    padding: 15px;
    border-radius: 5px;
    margin-top: 20px;
}

/* Styling list items */
li {
    margin-bottom: 10px;
    line-height: 1.6;
}

/* Adding spacing to <code> and <pre> */
pre code {
    font-family: Consolas, "Courier New", monospace;
    background-color: #eaeaea;
    padding: 5px;
    border-radius: 3px;
}

/* Add a smooth hover transition effect on lists */
li:hover {
    color: #fdfdfd;
    transition: color 0.3s ease;
}

/* Styles for the footer */
footer {
    background-color: #333; /* Dark background color */
    color: white; /* White text color */
    text-align: center; /* Center the text */
    padding: 15px; /* Internal spacing */
    position: relative; /* Relative positioning for correct behavior */
    bottom: 0; /* Align to the bottom */
    width: 100%; /* Full width */
    margin-top: 20px; /* Top margin to separate from previous content */
    border-top: 3px solid #4CAF50; /* Green top border */
}

/* Style for footer text */
footer p {
    margin: 0; /* No additional margins */
    font-size: 0.9rem; /* Smaller font size */
}
